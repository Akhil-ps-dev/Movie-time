import 'package:flutter/material.dart';

class ScreenDowloads extends StatelessWidget {
  const ScreenDowloads({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Downloads'),
      ),
    );
  }
}
