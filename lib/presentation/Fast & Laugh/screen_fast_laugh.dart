import 'package:flutter/material.dart';

class ScreenFastandLaugh extends StatelessWidget {
  const ScreenFastandLaugh({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Fast and laugh'),
      ),
    );
  }
}
